<!DOCTYPE html>
<html>
  <head>
  <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%=@page_title%></title>
    <%= csrf_meta_tags %>
    <meta name="keywords" content= "<%= @seo_keywords %>"/>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'portfolios', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
  <div class='container'>
  
    <%= render 'shared/port_nav'%>

    <main role="main">
     <section class="jumbotron text-center">
        <div class="container">
          <h1 class="jumbotron-heading">My Portfolios</h1>
          <p class="lead text-muted">A short sample of some projects I have worked on or created. Github repos are works in progress!</p>
          <p>
            <%=  link_to link_to "Resume", pages_about_path, class: " btn btn-primary my-2"%>
            <%=  link_to link_to "Blogs", blogs_path, class: " btn btn-secondary my-2"%>
          </p>
        </div>
      </section>
          
        <%= yield %>
        <!--FOOTER-->
         <%=render "shared/portfolio_footer"%>
            <%=source_helper%>
    </main>
    </div>
  </body>
</html>