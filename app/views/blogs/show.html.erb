<div class="blog-post">
            <h2 class="blog-post-title"><%= @blog.title %></h2>
              <%= link_to 'Edit', edit_blog_path(@blog)  if logged_in?(:site_admin) %> 
                <p class="blog-post-meta"> <%= @blog.body %></p>

<%= link_to 'Back', blogs_path %>
</div>
</div>
